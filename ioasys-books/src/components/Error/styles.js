import { Alert } from 'antd';
import styled from 'styled-components';
import Colors from '../../styles/colors';

const Container = styled(Alert)`
  background-color: ${Colors.backgroundErrorContainer};
`;

export default Container;
